# NPM Configuration
# Optimized for development workflow

# ===== GLOBAL INSTALL DIRECTORY =====

# Install global packages to ~/.npm-global instead of system directory
# Requires adding ~/.npm-global/bin to PATH
prefix=${HOME}/.npm-global

# ===== PACKAGE INSTALLATION =====

# Save installed packages with exact version (not ^ or ~)
save-exact=true

# Require package-lock.json to match package.json
# Fails if dependencies in package.json don't match lock file
engine-strict=true

# ===== PACKAGE METADATA =====

# Disable package funding messages
fund=false

# Disable audit during install (run manually with `npm audit`)
# Useful for CI/CD to reduce noise
audit=false

# Update notifier - check for npm updates
update-notifier=true

# ===== PERFORMANCE =====

# Enable progress bar
progress=true

# Maximum number of concurrent HTTP requests
maxsockets=50

# Fetch timeout (milliseconds)
fetch-timeout=60000

# ===== PACKAGE PUBLISHING =====

# Default tag when publishing (e.g., npm publish)
tag=latest

# Default access when publishing scoped packages
# Options: public, restricted
# access=public

# ===== REGISTRY =====

# Default registry (uncomment to use custom registry)
# registry=https://registry.npmjs.org/

# ===== LOGGING =====

# Log level: silent, error, warn, notice, http, timing, info, verbose, silly
loglevel=warn

# ===== CACHE =====

# Cache location (default: ~/.npm)
# cache=${HOME}/.npm

# Cache verification frequency
# Options: always, never, auto
cache-min=10

# ===== SCRIPTS =====

# Run scripts in foreground (see output immediately)
foreground-scripts=true

# Ignore scripts (for security, uncomment if needed)
# ignore-scripts=true

# ===== SECURITY =====

# Disable automatic execution of lifecycle scripts
# Uncomment for enhanced security in CI/CD
# ignore-scripts=true

# ===== MISCELLANEOUS =====

# Save packages to dependencies (not devDependencies) by default
save=true

# Allow running npm as root (disable in production)
# unsafe-perm=true

# Prefer offline mode (use cache when possible)
# prefer-offline=true

# Prefer online mode (always fetch fresh)
# prefer-online=false

# Legacy peer dependencies (for compatibility with older packages)
# legacy-peer-deps=false

# ===== DEVELOPMENT =====

# Show detailed timing information
# timing=true

# Enable long stack traces
# long=true